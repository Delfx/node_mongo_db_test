<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>MY APP</title>
</head>
<body>
    <h3>Find Customer Order</h3>
    <ul class="customerOrder">
        <% for(var i = 0; i < customerOrder.length; i++) { %>
        <li class="product">
            <span><%= customerOrder[i].products %></span>
        </li>
        <% } %>
    </ul>
</body>
</html>